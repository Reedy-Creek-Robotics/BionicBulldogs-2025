---@class turret
turret = {}

---@param reset boolean
function turret.init(reset) end

function turret.start() end

function turret.reset() end

---@param angle number
function turret.turnTo(angle) end

---@param angle number
function turret.turnAngle(angle) end

---@param angle number
function turret.update(angle) end